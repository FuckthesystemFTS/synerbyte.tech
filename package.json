{
  "name": "synerchat",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app/frontend && npm install --include=dev --legacy-peer-deps && npm run build && cd ../.. && mkdir -p app/backend/static && cp -r app/frontend/dist/* app/backend/static/",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
