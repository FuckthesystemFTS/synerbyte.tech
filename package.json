{
  "name": "synerchat",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app/frontend && npm install --include=dev && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
